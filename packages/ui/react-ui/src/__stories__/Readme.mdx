import { Meta } from '@storybook/blocks';

<Meta title="Bridge Modal Demo" />

# BridgeModal

Demo/Default story describing the usage of the BridgeModal component.

To use it properly, create a project ID at [WalletConnect](https://cloud.walletconnect.com).

The best way to see the demo is by pasting the project ID via Storybook's controls and navigating to the separate component page: [./iframe.html?args=walletConnectProjectId:$PROJECT_ID&id=demo--default&viewMode=story](./iframe.html?args=walletConnectProjectId:$PROJECT_ID&id=demo--default&viewMode=story).
