import it from "ava";
//import { DeBridgeBridgeAdapter } from "../lib/BridgeAdapter/DeBridgeBridgeAdapter";

it("should be initialized", (t) => {
  //const adapter = new DeBridgeBridgeAdapter();

  t.is(1, 1);
});
